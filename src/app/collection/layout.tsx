import SpineFontScope from "@/components/scene-assets/books/SpineFontScope";

export default function CollectionLayout({ children }: Readonly<{ children: React.ReactNode }>) {
  return <SpineFontScope>{children}</SpineFontScope>;
}
