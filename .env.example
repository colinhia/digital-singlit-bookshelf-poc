# Neon/PostgreSQL connection string used by /collection and /my-library.
# Keep the real value in .env.local or the deployment environment.
DATABASE_URL=postgresql://USER:PASSWORD@HOST/DATABASE?sslmode=require
